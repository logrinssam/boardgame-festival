/**
 * 개발용 예약 테스트 설정.
 * 실제 boothData의 capacity / waitlistCapacity에는 값을 넣지 않는다.
 * DEMO_MODE가 true이면 미설정 부스에도 데모 정원이 적용되고 UI에 데모 표시가 뜬다.
 */
export const DEMO_MODE = false;
export const DEMO_CAPACITY = 6;
export const DEMO_WAITLIST_CAPACITY = 2;
